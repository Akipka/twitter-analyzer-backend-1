# Production build env. Vite reads this during `vite build`.
# To override, set VITE_API_URL in Vercel Project → Settings → Environment Variables.
VITE_API_URL=https://twitter-analyzer-backend.onrender.com
